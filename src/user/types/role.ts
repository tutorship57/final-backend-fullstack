export type Role = 'user' | 'admin' | 'manager' | 'merchant' | 'editor';

export enum RoleEnum {
  USER = 'user',
  ADMIN = 'admin',
  MANAGER = 'manager',
  MERCHANT = 'merchant',
  EDITOR = 'editor',
}
